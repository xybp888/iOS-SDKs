framework module SwiftUICore [system] {
    umbrella header "SwiftUICore.h"
    export *
    module * {export *}

    // Exposes `environment.containerBackgroundLuminance` for SaltUICore to read.
    // FIXME: Remove when this lands in the build. rdar://168763340
    module Radar160700024 {
    }

    // FIXME: remove this once .intelligence() has been in the build and UIKit
    // has removed their canImport.
    module Radar168044986 {
    }

    // FIXME: Remove when _Glass.SiriWaveOptions has been in the build and
    // DesignLibrary/UIKit have removed their canImport checks.
    module Radar169985572 {
    }

    module Radar174504683 {
    }

    // FIXME: Remove once this has been in the build and UIKit, DesignLibrary
    // has removed dependencies.
    // - diffuseHeightScale
    // - diffuseSpreadScale
    // - diffuseAmountScale
    module Radar169615781 {
    }

    // Remove with rdar://170272324 after canImport checks in UIKit have been
    // removed.
    module Radar170141711 {
    }

    // FIXME: Remove once UIKit adopts
    module Radar170560909 {
    }

    // FIXME: Remove once DesignLibrary, UIKit and AppKit have removed their
    // canImport checks for `_Glass.BackgroundContent`.
    module Radar172291289 {}

    // FIXME: Remove once DesignLibrary have removed dependencies for canImport
    // checks for `.forceColorResolution`.
    module Radar172795380 {}

    // FIXME: Remove once AppKit and DesignLibrary have removed their
    // canImport checks for `windowIsFullScreen`.
    module Radar173438976 {}

    // FIXME: Remove once UIKit has removed canImport checks for
    // TypesettingConfigurationKey/AppearsActiveKey/related symbols
    module Radar172777090 {}

    // FIXME: Remove once UIKit has removed canImport checks for the
    // ReduceDesktopTintingKey trait bridge (rdar://180542021).
    module Radar180542021 {}

    // FIXME: Remove once AppKit, UIKit and DesignLibrary have removed their
    // canImport checks for `.translucent` and `.uniform(...)`
    module Radar173867784 {}

    // FIXME: Remove once DesignLibrary has removed their canImport checks for
    // .opacity
    module Radar174379416 {}

    // FIXME: Remove once UIKit has removed canImport checks for
    // .gradientContour
    module Radar175136758 {}

    // FIXME: Remove once DesignLibrary + UIKit have removed their canImport
    // checks for _Glass.BackgroundContent.dimmed
    module Radar175699312 {}

    // FIXME: Remove once UIKit has removed canImport checks for reading
    // FlexInteraction.ParameterSet. In the future this should be further 
    // replaced by PrototypeTools (PT linked to SwiftUICore and PTSettings 
    // defined in SwiftUICore and read by both SwiftUICore and UIKit)
    module Radar175256109 {}

    // FIXME: Remove once DesignLibrary + UIKit have removed their canImport
    // checks for _Glass.debugIdentifier
    module Radar175864265 {}

    // FIXME: Remove once UIKit + AppKit have removed their canImport checks
    // for MaterialLuminanceAggregator.
    module Radar176105619 {}

    // FIXME: Remove once UIKit + AppKit have removed their canImport checks
    // for MaterialLuminanceAggregator.update(colorScheme:)
    module Radar176848451 {}

    // FIXME: Remove once DesignLibrary has removed its canImport checks
    // for _Glass.vibrantFill.
    module Radar176827131 {}

    // FIXME: Remove once UIKit has removed its canImport checks for
    // _Glass.isIdentity.
    module Radr177070692 {}

    // FIXME: Remove once DataDetection+SwiftUI achieve symbiosis.
    module Radar177365908 {}

    // FIXME: Remove once DesignLibrary has removed its canImport checks for SiriWaveOptions.AudioMeters
    module Radar177967723 {}

    // FIXME: Remove once DesignLibrary has removed its canImport checks
    // for _Glass.minimumDimension.
    module Radar170265311 {}

    // FIXME: Remove once UIKit has removed its canImport check for
    // EnvironmentValues.MaxAllowedSystemUIContentHeadroomKey.
    module Radar168022170 {}

    // FIXME: Remove once AppKit has removed its canImport checks in
    // `_NSCoreHostingView`.
    module Radar181829596 {}

    // FIXME: Remove once AppKit has removed its canImport checks for
    // _Glass.interactive(_:variant:sources:) / FlexInteraction.Sources.
    module Radar175663406 {}

    // Remove with rdar://182803810 after canImport checks in UIKit and AppKit
    // have been removed.
    module Radar182507950 {}

    // Remove with rdar://182832913 after canImport checks in AppKit have been
    // removed.
    module Radar182831521 {}
}
